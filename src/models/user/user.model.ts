/**
 * @description This code defines the interface of a User model, specifying its properties as id, name, email, password, and createdAt.
 */
export interface UserModel {
  id: string;
  name: string;
  email: string;
  password: string;
  createdAt: string;
}
